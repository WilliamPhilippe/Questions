#include <bits/stdc++.h>
using namespace std;

long long int *vetor = (long long int *) malloc(300000 * sizeof(long long int));

int main(){


	cout << (300000 * sizeof(long long int) ) / (8*1024);

	return 0;
}